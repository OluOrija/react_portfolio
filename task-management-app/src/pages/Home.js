// src/pages/Home.js
import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Welcome to the Task Management App</Typography>
      <Typography variant="body1">Manage your tasks efficiently and track progress.</Typography>
    </div>
  );
};

export default Home;
